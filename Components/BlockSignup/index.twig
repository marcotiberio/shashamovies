<div class="flyntComponent componentSpacing {{ options.theme }}" is="flynt-block-signup">
  <div class="container container--fullWidth">
    <div class="content">
      {{ contentHtml|e('wp_kses_post') }}
    </div>
    <a class="button" href="{{ button.url }}">{{ button.title }}</a>
  </div>
</div>
